import styled from "styled-components";

export const LabelText = styled.p.attrs({
  className: "text-black-40 text-body font-semibold",
})``;

export const InfoText = styled.p.attrs({
  className: "text-H2 font-bold",
})``;

import styled from "styled-components";

const ButtonContainer = styled.button.attrs({
  className:
    "bg-primary px-12 py-3.5 border-transparent text-Body font-medium	text-white rounded-full hover:bg-info-hover",
})``;

export default ButtonContainer;

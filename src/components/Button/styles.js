import styled from "styled-components";

const ButtonContainer = styled.button.attrs({
  className: "bg-primary px-8 py-3 text-H4 font-medium	 text-white rounded-full",
})``;

export default ButtonContainer;

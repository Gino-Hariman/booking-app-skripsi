import styled from "styled-components";

export const ProfileContainer = styled.div.attrs({
  className:
    "flex flex-col min-h-screen flex-1 py-20 items-center justify-center",
})``;

export const ProfileDataGroup = styled.div.attrs({
  className: "w-full flex flex-col items-center justify-center",
})``;

export const ProfileFieldContainer = styled.div.attrs({
  className: "flex w-1/2 flex-col border-b mb-8 border-black-10 ",
})``;
